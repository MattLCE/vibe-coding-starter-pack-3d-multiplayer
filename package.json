{
  "name": "vibe-multiplayer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && spacetime start && cd ../client && npm run preview",
    "dev": "cd server && spacetime start & cd ../client && npm run dev"
  },
  "dependencies": {
    "chart.js": "^4.4.8",
    "react-chartjs-2": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
